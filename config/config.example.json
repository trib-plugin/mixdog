{
  "__comment": "Global plugin configuration. Seeded automatically on first install — edit via /mixdog:config or directly in the data directory.",
  "promptInjection": {
    "__comment": "Controls how mixdog injects rules and memory into the session. 'claude_md' writes to CLAUDE.md (recommended); 'hook' uses the session-start hook output only.",
    "mode": "claude_md",
    "targetPath": "~/.claude/CLAUDE.md"
  }
}

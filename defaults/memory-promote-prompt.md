Phase: {{PHASE}}

## Current active core

{{CORE_MEMORY}}

## Entries to Evaluate

{{ITEMS}}

Active count: {{ACTIVE_COUNT}} / cap: {{ACTIVE_CAP}}.

---
Output format: a single JSON object as the first and only content, schema `{"actions":[...]}`. No prose, no preamble, no markdown code fence. An empty result is `{"actions":[]}`. Start the reply with `{`.
